<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

    <mapper namespace="com.moijo.gomatch.domain.meeting.mapper.MemberMapper">
    <select id="checkLogin" resultType="com.moijo.gomatch.domain.member.vo.MemberVO">
        SELECT * FROM MEMBER WHERE MEMBER_ID = #{memberId} AND MEMBER_PW = #{memeberPw}
    </select>


    </mapper>

